# My Portfolio

Minimal Next.js (app dir) portfolio scaffold.

Quick start

```bash
npm install
npm run dev
```

This creates a simple portfolio with a hero section. Edit `src/app/page.tsx` and `src/components/Hero.tsx` to customize.
